export default function productReducer(product, action) {
    switch (action.type) {
        case '':

            break;

        default:
            alert('No matching types!');
            return product;
    }
}